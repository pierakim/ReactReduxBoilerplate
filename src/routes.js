/*
import React from 'react';
import {Route} from 'react-router'; //specific components from react router
import App from './components/App'; //we define it
import HomePage from './components/home/HomePage'; //we define it
import AboutPage from './components/about/AboutPage'; //we define it

export default(
    <Route path = "/" component={App}>
        <Route exact path="/" component={HomePage}/> /!*home Page - This is what we pass as children to App.js*!/
        <Route path="about" component={AboutPage} />/!*About Page - This is what we pass as children to App.js*!/
    </Route>
);
*/
